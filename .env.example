# This file is for reference only. It is never read by the application.
# Copy it to .env and fill in real values before running docker compose:
#   cp .env.example .env
#
POSTGRES_DB=myvibereader
POSTGRES_USER=myvibereader
POSTGRES_PASSWORD=myvibereader
JWT_SECRET=change-me-use-a-random-256-bit-value
